# 视频接入与AI分析系统架构

## 1. 文档目的

本系统需要统一接入电脑桌面、电脑外接摄像头、网络摄像头以及不同品牌的独立摄像头，提供实时观看、录像、人工抽帧查看和多种AI分析能力。

总体原则是：

- 所有设备的视频先统一进入SRS流媒体服务。
- MediaService负责视频源、录像和AI配置管理。
- AIService负责实际的视频分析，不负责设备品牌适配和录像控制。
- 同一路视频在一个AI节点只拉取、解码一次，再交给多个算法模块使用。
- 打架、安全帽、火灾以及后续算法使用统一的配置、任务、事件和证据模型。

## 2. 总体架构

```text
电脑桌面 ─────────────┐
电脑外接USB摄像头 ────┤
电脑连接RTSP摄像头 ───┤
品牌摄像头RTMP直推 ───┤
不支持推流的摄像头 ───┘ 由APP/NVR/边缘网关拉RTSP后转推
                         │
                         ▼
                    SRS流媒体服务
                         │
          ┌──────────────┼──────────────┐
          │              │              │
          ▼              ▼              ▼
      实时观看       MediaService     AIService
                       │                │
                       │                ├─ 共享拉流与解码
                       │                ├─ 录像抽帧
                       │                ├─ 打架检测
                       │                ├─ 安全帽检测
                       │                ├─ 火灾检测
                       │                └─ 后续算法
                       │                     │
                       └───────────◄─────────┘
                             AI事件和证据上报
```

系统的数据分为两类：

```text
MySQL：设备、视频源、录像索引、AI规则、任务、事件、处理状态
共享磁盘/NAS：录像文件、抽帧图片、告警截图、告警短视频
```

## 3. 各服务职责

### 3.1 SRS流媒体服务

SRS是所有实时视频的统一入口和分发中心，负责：

- 接收电脑APP和摄像头推送的RTMP视频。
- 将同一路视频分发给观看端、MediaService和AIService。
- 提供RTMP、HTTP-FLV/HLS等内部或观看协议。
- 隔离上游设备差异，让下游服务只处理标准视频流。

SRS不负责：

- 设备信息管理。
- 录像保留策略。
- AI算法配置。
- 异常行为判断。

### 3.2 MediaService

目录：`D:\project\eyes\mediaService`

MediaService是系统控制中心，负责：

- 电脑和视频源登记。
- 为视频源生成唯一、稳定的`stream_name`。
- 提供品牌摄像头RTMP推流地址。
- 查询SRS中的在线流。
- 控制全局录像开启或关闭。
- 从SRS拉流并按时间分段录制MP4。
- 管理录像和图片索引、保留天数及过期清理。
- 保存每个视频源启用哪些AI算法及算法参数。
- 创建离线AI任务并接收AIService执行结果。
- 保存AI Worker心跳、AI异常事件和人工复核状态。
- 提供统一管理后台。

MediaService不负责执行深度学习模型。录像完整性检查仍可使用FFprobe，但抽帧和未来AI推理由AIService执行。

### 3.3 AIService

目录：`D:\project\eyes\AIService`

AIService是统一的AI分析平台，负责：

- 向MediaService报告Worker心跳和可用算法能力。
- 从MediaService获取需要执行的任务和视频源AI配置。
- 从SRS自动拉取需要分析的在线流。
- 负责断线重连、解码、采样和短期帧缓存。
- 将解码结果分发给启用的算法模块。
- 执行录像抽帧、打架、安全帽、火灾等算法。
- 进行连续命中、告警冷却和事件合并。
- 生成截图、告警前后短视频等证据。
- 向MediaService上报结构化AI事件。

AIService不负责：

- 为摄像头生成推流地址。
- 保存设备品牌账号或摄像头后台配置。
- 控制MediaService是否录制完整录像。
- 直接修改MediaService的MySQL数据。

## 4. 多种设备如何接入

系统使用统一的`VideoSource`描述视频来源。主要来源类型为：

| 来源类型 | 示例 | 接入方式 |
|---|---|---|
| `screen` | 电脑桌面 | 电脑APP采集并推送RTMP |
| `usb_camera` | 电脑外接USB摄像头 | 电脑APP调用FFmpeg采集并推送RTMP |
| `ip_camera` | 电脑能够访问的RTSP摄像头 | 电脑APP拉取RTSP并转推RTMP |
| `direct_camera` | 独立品牌摄像头 | 摄像头后台直接填写系统提供的RTMP地址 |

所有视频源最终都必须转换成：

```text
rtmp://<SRS地址>:1935/live/<stream_name>
```

下游录像和AI分析只识别`stream_name`，不关心视频来自电脑桌面、海康、大华、宇视或其他品牌。

如果摄像头只支持RTSP拉流、不支持主动RTMP推流，需要由以下任意组件完成协议转换：

- 部署在现场电脑上的现有APP。
- NVR。
- 独立边缘网关。
- 后续增加的摄像头接入网关服务。

不建议让每一种AI算法直接连接不同品牌摄像头，否则账号、协议、重连和品牌兼容问题会进入所有AI模块。

## 5. 录像控制逻辑

录像开关仍然由MediaService控制：

```http
GET /api/recording-settings
PUT /api/recording-settings
```

### 开启录像

```text
设备推流 -> SRS -> MediaService录制MP4
                   └-> 录像片段入库后创建frame_sampler任务
```

### 关闭录像

```text
设备继续推流 -> SRS继续接收
                  ├-> 实时观看正常
                  ├-> MediaService停止生成新录像
                  └-> 实时AI分析仍然可以运行
```

当前AIService的第一个模块`frame_sampler`以已完成的录像片段作为输入。因此关闭录像后不会产生新的录像片段，也不会产生新的抽帧任务。

未来打架、安全帽、火灾等实时算法直接分析SRS实时流，不受录像开关影响。是否保存异常短视频由AI规则单独控制。

## 6. AI算法配置逻辑

业务人员在MediaService管理后台选择具体摄像头，然后配置需要启用的算法。配置保存在MediaService，AIService只读取和执行。

示例：

| 视频源 | 打架 | 安全帽 | 火灾 |
|---|---:|---:|---:|
| 学校操场摄像头 | 开启 | 关闭 | 关闭 |
| 工地入口摄像头 | 关闭 | 开启 | 关闭 |
| 仓库内部摄像头 | 关闭 | 关闭 | 开启 |
| 工厂车间摄像头 | 开启 | 开启 | 开启 |
| 电脑桌面 | 关闭 | 关闭 | 关闭 |

每条规则至少包含：

```json
{
  "video_source_id": 12,
  "algorithm_code": "helmet",
  "enabled": true,
  "config": {
    "threshold": 0.80,
    "sample_fps": 2,
    "cooldown_seconds": 60,
    "schedule": "08:00-18:00",
    "roi": [[100, 100], [1500, 100], [1500, 900], [100, 900]]
  }
}
```

字段含义：

- `threshold`：模型判断阈值。
- `sample_fps`：该算法每秒分析多少帧。
- `cooldown_seconds`：同类事件的重复告警冷却时间。
- `schedule`：算法生效时段。
- `roi`：只分析画面中的指定区域。

规则表使用通用`algorithm_code + config_json`结构，因此增加新算法时不需要为每个算法新建一套配置表。

## 7. AIService共享拉流与解码

错误方式：

```text
打架服务单独拉取摄像头A
安全帽服务再次拉取摄像头A
火灾服务第三次拉取摄像头A
```

对于75路视频和3种算法，上述方式可能产生225路拉流和解码任务。

正确方式：

```text
摄像头A -> 一个StreamSession -> 一次拉流和解码
                                ├-> 安全帽：每秒取2帧
                                ├-> 火灾：每秒取2～4帧
                                └-> 打架：读取连续2～4秒帧缓存
```

AIService按照视频流分配任务，而不是按照算法重复拉流。每个StreamSession负责：

- 拉流和自动重连。
- 视频解码。
- 最新帧缓存。
- 连续视频环形缓存。
- 根据算法采样频率分发帧。
- 统计丢帧、解码错误和分析延迟。

多GPU扩容时，以视频流为单位将StreamSession分配到不同Worker，确保同一视频流在同一节点只解码一次。

## 8. 不同AI模块的输入方式

| 算法 | 输入 | 建议频率 | 说明 |
|---|---|---:|---|
| 录像抽帧 | 已完成MP4片段 | 每片段2张起 | 用于人工浏览，不是实时检测输入 |
| 打架检测 | 连续视频序列 | 4～8 FPS | 需要分析连续2～4秒动作 |
| 安全帽检测 | 单帧或短连续帧 | 1～2 FPS | 需要人员、头部和安全帽关联 |
| 火灾检测 | 连续图片/视频 | 2～4 FPS | 建议同时识别烟雾和火焰并连续确认 |

不同算法共享解码结果，但可以拥有不同的采样频率、阈值、ROI和事件规则。

## 9. AI事件处理

AI模块不能只命中一帧就立即认定发生异常，统一事件流程为：

```text
模型首次命中
     |
连续多帧或多个窗口达到阈值
     |
合并成一个AI事件
     |
保存截图和告警前后短视频
     |
上报MediaService
     |
后台显示“待确认”
     |
人工选择“确认”或“误报”
```

通用事件应包含：

```text
event_id
video_source_id
stream_name
algorithm_code
event_type
confidence
started_at / ended_at
snapshot_path
clip_path
model_version
status: pending / confirmed / false_positive
metadata_json
```

人工确认结果需要保留，后续可作为现场模型优化和误报分析数据。

## 10. 数据模型

当前系统已经建立以下通用模型：

- `video_sources`：所有视频来源的统一登记信息。
- `recording_settings`：全局录像开关和录像保留时间。
- `recording_segments`：录像片段索引。
- `recording_frames`：人工查看图片索引。
- `ai_algorithms`：AI算法能力目录。
- `video_analysis_rules`：每个视频源的算法开关和参数。
- `ai_jobs`：可重试、可租约领取的离线AI任务。
- `ai_workers`：AIService Worker能力和心跳。
- `ai_events`：打架、安全帽、火灾及后续算法的统一事件表。

视频文件和图片文件不能直接存入MySQL。MySQL只保存路径和元数据，文件保存在共享磁盘、NAS或后续对象存储。

## 11. 服务接口边界

### 当前已实现

| 接口 | 用途 |
|---|---|
| `GET /api/streams` | 查询当前在线流 |
| `GET/PUT /api/recording-settings` | 查询或控制录像 |
| `GET /api/ai/algorithms` | 查询AI算法目录 |
| `GET /api/ai/jobs/stats` | 查询AI任务和Worker状态 |
| `POST /api/internal/ai/jobs/claim` | AIService领取离线任务 |
| `POST /api/internal/ai/jobs/report` | AIService上报任务结果 |
| `POST /api/internal/ai/workers/heartbeat` | AIService上报心跳 |

### 后续需要实现

| 接口 | 用途 |
|---|---|
| `GET/PUT /api/video-sources/{id}/ai-rules` | 管理某个视频源的AI规则 |
| `GET /api/internal/ai/realtime-config` | AIService同步在线流和实时算法配置 |
| `POST /api/internal/ai/events` | AIService上报实时异常事件 |
| `GET /api/ai/events` | 后台查询异常事件 |
| `PUT /api/ai/events/{id}` | 人工确认或标记误报 |

内部AI写接口不使用Token时，必须通过Docker内部网络、防火墙、反向代理访问规则或可信局域网限制访问，不能直接向公网开放。

## 12. 故障隔离

### AIService停止

- 设备推流不受影响。
- 实时观看不受影响。
- MediaService录像不受影响。
- 离线抽帧任务保留在MySQL，AIService恢复后继续处理。
- 实时AI检测暂停。

### MediaService关闭录像

- 推流和观看继续。
- 不再生成完整录像和新的录像抽帧任务。
- 未来实时AI检测继续。

### 单个AI算法异常

- 只停止或降级对应算法。
- 其他算法继续使用同一视频流。
- Worker上报错误和心跳，后台显示故障状态。

### SRS停止

- 上游推流、实时观看、录像和实时AI都会中断。
- SRS是媒体链路核心，应配置自动重启和健康检查。

## 13. 部署结构

第一阶段：

```text
mysql
srs
media-service
ai-service
```

AIService和MediaService共享`/var/recordings`存储卷。MediaService使用22222端口，AIService使用11111端口。

当前发布由`mediaService/build.sh`统一构建和部署两个服务，执行时选择目标：

- `1`：公网`test@112.18.238.6:2202`。
- `2`：内网`test@10.0.20.219:20`。

两台服务器均无GPU。现有录像、抽帧、任务调度和管理功能可以使用CPU运行；Qwen视觉
API可用于低频抽帧、候选事件的二次复核和自然语言说明。它不直接承担75路视频的逐帧
实时检测，避免网络上行、API并发、时延和费用成为主链路瓶颈。实时算法仍按
“共享解码 + 专用检测/跟踪/时序模型 + Qwen可选复核”演进。每台服务器在自己的
`.env`中保存`DASHSCOPE_API_KEY`，构建脚本不得上传或覆盖生产密钥。

后续多GPU扩容：

```text
ai-manager
ai-worker-gpu-01
ai-worker-gpu-02
...
```

Worker通过心跳报告：

- Worker编号。
- 主机和GPU信息。
- 支持的算法列表。
- 当前任务数。
- 最近错误。
- 最后心跳时间。

调度器根据视频流数量、启用算法和GPU负载分配StreamSession。

## 14. 当前完成情况

已经完成：

- 多种视频来源的统一登记和稳定`stream_name`。
- 电脑桌面、USB摄像头、网络摄像头和品牌摄像头直推接入基础。
- MediaService全局录像开关。
- 独立AIService项目和模块注册机制。
- 将原MediaService抽帧执行迁移为AIService的`frame_sampler`模块。
- 持久化AI任务、任务租约、超时恢复、失败重试和结果幂等入库。
- AI Worker心跳和管理后台状态展示。
- `fight`、`helmet`、`fire`算法目录及通用规则、事件数据结构。
- Docker Compose和离线构建部署脚本支持AIService。

尚未完成：

- 按摄像头配置AI算法的管理接口和后台页面。
- AIService实时在线流同步。
- 每路视频只解码一次的StreamSession和环形缓存。
- 打架、安全帽和火灾的实际模型及推理代码。
- 实时事件上报、告警证据和人工确认页面。
- 75路视频的GPU压力测试和容量评估。

## 15. 推荐后续实施顺序

1. 实现每个视频源的AI规则管理接口和后台页面。
2. 实现AIService实时配置同步和StreamSession。
3. 实现共享解码、图片采样和时序环形缓存。
4. 先使用模拟算法跑通事件、截图、短视频和人工确认流程。
5. 优先实现禁区入侵、越线、逗留和人数统计，共用检测、跟踪与ROI能力。
6. 接入安全帽检测模型。
7. 接入烟雾和火焰检测模型。
8. 接入打架检测模型并使用连续视频片段验证。
9. 从5路、20路逐步压力测试到75路。
10. 根据实际GPU负载、误报和漏报调整采样频率、阈值及硬件数量。

以上架构保证增加新设备品牌时主要修改接入层，增加新AI能力时主要增加算法模块，两者不会相互耦合。

## 16. 可扩展的视频分析场景

视频分析能力应按技术类型建设，而不是每想到一个场景就重新开发一套拉流、解码、任务和事件系统。以下场景可以复用统一AI平台。

| 分类 | AI服务 | 主要技术 | 现场定制难度 |
|---|---|---|---:|
| 周界安全 | 禁区入侵 | 目标检测+跟踪+ROI | 低 |
| 周界安全 | 越线检测 | 跟踪+虚拟线 | 低 |
| 周界安全 | 反方向通行 | 跟踪+方向规则 | 低 |
| 周界安全 | 长时间逗留 | 跟踪+停留时间 | 低 |
| 周界安全 | 非工作时间出现人员 | 人员检测+时段规则 | 低 |
| 人群管理 | 人数统计 | 人员检测+跟踪 | 低 |
| 人群管理 | 人员聚集/超员 | 人数+区域阈值 | 低 |
| 人群管理 | 排队长度和等待时间 | 跟踪+计时 | 中 |
| 物品安全 | 遗留物品 | 目标跟踪+背景变化 | 中 |
| 物品安全 | 物品被移走 | 目标检测+场景基线 | 中 |
| 人员安全 | 人员跌倒 | 姿态/时序动作模型 | 中高 |
| 人员安全 | 打架 | 时序动作模型 | 高 |
| 人员安全 | 攀爬、翻越围栏 | 姿态+时序规则 | 高 |
| 作业安全 | 安全帽检测 | 人员+头部+安全帽检测 | 中 |
| 作业安全 | 反光衣/工作服检测 | PPE检测 | 中 |
| 作业安全 | 抽烟检测 | 手部、香烟和烟雾联合判断 | 高 |
| 作业安全 | 危险区域有人作业 | 人员检测+ROI | 低 |
| 消防环境 | 火焰检测 | 火焰模型+连续确认 | 中高 |
| 消防环境 | 烟雾检测 | 烟雾模型+时序确认 | 高 |
| 消防环境 | 消防通道被占用 | 目标检测+区域规则 | 中 |
| 环境监控 | 积水/水浸 | 场景分割或状态分类 | 中高 |
| 车辆管理 | 车辆计数、进出方向 | 车辆检测+跟踪 | 低 |
| 车辆管理 | 违停 | 车辆检测+停留规则 | 中 |
| 车辆管理 | 拥堵和排队 | 数量+速度+区域规则 | 中 |
| 车辆管理 | 车牌识别 | 车牌检测+OCR | 中 |
| 设备监控 | 设备运行/停止 | 状态分类或运动检测 | 中 |
| 设备监控 | 仪表读数 | 仪表定位+OCR/指针识别 | 中高 |
| 设备监控 | 传送带堵料、跑偏 | 专用检测/分割模型 | 高 |
| 视频质量 | 摄像头遮挡 | 画面变化检测 | 低 |
| 视频质量 | 画面冻结、黑屏、模糊 | 图像质量规则 | 低 |
| 视频质量 | 摄像头被移动 | 场景基线比较 | 低 |

“难度低”不表示完全不需要现场调试，而是通常可以先使用通用预训练检测模型，通过ROI、时段、持续时间和阈值完成第一版。“难度高”通常表示必须使用连续视频、姿态或专用模型，并准备现场标注数据。

## 17. 禁区入侵和异物闯入

建议把“异物闯入”拆成两个明确需求。

### 17.1 指定类型目标进入禁区

检测人、汽车、叉车、动物、包裹等已知目标是否进入禁止区域。这是推荐优先实现的生产方案。

```text
目标检测
   ↓
为目标分配跟踪ID
   ↓
判断目标中心点是否进入ROI多边形
   ↓
判断是否达到最小面积和持续时间
   ↓
生成禁区入侵事件
```

示例规则：

```json
{
  "algorithm_code": "intrusion",
  "enabled": true,
  "config": {
    "roi": [[120, 80], [1500, 80], [1500, 900], [120, 900]],
    "object_types": ["person", "vehicle", "animal"],
    "min_duration_seconds": 2,
    "minimum_object_area": 1000,
    "schedule": "22:00-06:00",
    "cooldown_seconds": 60
  }
}
```

需要支持的配置包括：

- 一台摄像头可以绘制多个ROI。
- ROI可以是禁入区，也可以是只允许特定目标进入的区域。
- 可以限制人员、车辆、动物或指定类别。
- 可以按工作时段和非工作时段配置不同规则。
- 目标必须持续存在一定时间，过滤飞虫、反光和瞬间噪声。
- 使用最小目标面积过滤远处小目标和压缩噪声。
- 同一目标持续停留时合并为一个事件，不重复报警。

NVIDIA DeepStream的分析插件也采用检测、跟踪和规则的组合，提供ROI、越线、方向和拥挤判断，可作为实现设计参考：[DeepStream ROI与越线分析文档](https://docs.nvidia.com/metropolis/deepstream/7.1/text/DS_plugin_gst-nvdsanalytics.html)。

### 17.2 任意未知物体或异常进入

如果需求是“无论是什么，只要画面中突然出现以前没有的东西就报警”，则属于开放世界检测或视频异常检测，难度明显更高。

可能采用：

- 背景建模和前景变化检测。
- 开放词汇目标检测。
- 只学习固定摄像头正常画面的异常检测模型。
- 通用检测失败后的视觉大模型二次判断。

这类方案容易受到光影、雨雪、树叶、飞虫、摄像头晃动、夜间红外切换和正常业务变化影响，应作为辅助发现能力，不能直接替代明确类别和明确规则的禁区入侵。

开放词汇检测可以通过“叉车”“纸箱”“动物”等文字提示发现未为当前项目单独训练的类别，但零样本结果仍需现场验证：[Grounding DINO论文](https://arxiv.org/abs/2303.05499)。视频异常检测研究也强调不同场景、光照、天气和数据集质量对结果有显著影响：[视频异常检测综述与MSAD数据集](https://arxiv.org/abs/2402.04857)。

## 18. 大模型与专用模型的边界

所有场景都可以使用人工智能技术辅助，但不应理解为“一个大模型持续观看75路视频并处理所有问题”。生产系统应采用分层架构：

```text
第一层：专用小模型快速检测
人员、车辆、安全帽、烟雾、火焰、运动目标
                 ↓
第二层：跟踪和确定性规则
ROI、越线、持续时间、时段、连续命中、告警冷却
                 ↓
第三层：视觉大模型按需复核
解释画面、判断低置信度事件、生成事件描述
                 ↓
人工确认
```

### 适合专用模型和规则的功能

- 禁区入侵。
- 越线和方向检测。
- 人员/车辆计数。
- 人员聚集和逗留。
- 安全帽和反光衣检测。
- 黑屏、冻结、遮挡和摄像头移动。
- 车辆违停。

这些功能需要高频、连续、低延迟运行，专用模型推理成本更低，输出坐标和置信度明确，也更容易配置告警阈值。

### 适合时序专用模型的功能

- 打架。
- 跌倒。
- 攀爬和翻越。
- 危险作业动作。
- 烟雾和火焰连续变化。

这些功能不能依赖单张图片，需要连续2～数秒的视频帧、姿态序列或运动特征。

### 视觉大模型适合承担的功能

- 对低置信度告警进行第二次判断。
- 用自然语言说明画面发生了什么。
- 快速试验尚未训练的新目标类别。
- 辅助人工标注和数据筛选。
- 按文字搜索历史告警。
- 综合多个检测结果生成事件摘要。

### 不应只依赖视觉大模型的功能

- 75路视频逐帧持续分析。
- 精确越线、计数和目标轨迹判断。
- 要求固定阈值和可重复结果的自动控制。
- 未经人工确认的高风险结论。
- 用同一个提示词和阈值适配所有摄像头。

## 19. 不同算法的数据与优化需求

### 19.1 可先使用预训练模型和规则

以下场景通常可以先用通用人员/车辆模型完成第一版：

- 指定目标禁区入侵。
- 越线。
- 人数统计。
- 人员聚集。
- 非工作时间出现人员。
- 车辆违停。

第一阶段重点不是重新训练，而是逐个摄像头调整ROI、时段、最小目标大小、持续时间和置信度，并积累现场误报。

### 19.2 安全帽和其他PPE

预训练模型可以作为起点，但需要根据现场安全帽颜色和样式、人员距离、摄像头角度、遮挡、夜间画面，以及普通帽子和反光物干扰进行优化。

应收集并标注：

- 人员。
- 头部。
- 戴安全帽。
- 未戴安全帽。
- 普通帽子、遮挡和远距离小目标等困难负样本。

使用现场自定义数据微调预训练检测模型是标准做法，可参考[MMDetection模型微调文档](https://mmdetection.readthedocs.io/en/3.x/user_guides/finetune.html)。

### 19.3 打架和复杂动作

需要准备连续视频片段，而不是单张图片。除了真实打架样本，还必须收集容易误判的负样本：

- 拥抱。
- 打球。
- 跑步。
- 拉扯物品。
- 人群拥挤。
- 正常肢体接触。

训练和验证数据需要覆盖不同机位、距离、白天、夜间和遮挡情况。可使用MMAction2的自定义视频数据集和预训练模型微调流程：[MMAction2自定义数据集](https://mmaction2.readthedocs.io/en/stable/advanced_guides/customize_dataset.html)、[MMAction2微调文档](https://mmaction2.readthedocs.io/en/latest/user_guides/finetune.html)。

### 19.4 火焰和烟雾

除正常火焰和烟雾外，需要重点积累以下困难负样本：

- 灯光和夕阳。
- 红色衣物和高亮反光。
- 焊接火花。
- 水蒸气、灰尘和云雾。
- 夜间红外切换。
- 视频压缩噪声。

火灾检测不应一帧命中就报警，应结合连续帧、面积变化、持续时间，以及烟雾和火焰的联合结果。

### 19.5 任意异常检测

可以使用固定摄像头数小时或数天的正常视频学习正常模式，再检测偏离正常模式的行为。但是“异常”的定义具有主观性和场景依赖性，适合发现未知问题，不适合作为唯一的确定性报警来源。

视频预训练研究也指出领域差异会影响迁移效果，数据质量可能比单纯增加数据数量更重要：[VideoMAE论文](https://arxiv.org/abs/2203.12602)。

## 20. 模型持续改进闭环

系统不能把每次人工操作直接用于自动在线训练，也不能让新模型未经验证自动替换生产模型。推荐流程为：

```text
AI产生待确认事件
       ↓
人工标记：确认 / 误报
       ↓
补录人工发现的漏报事件
       ↓
保存对应截图、短视频和运行参数
       ↓
筛选困难正样本和困难负样本
       ↓
人工审核并形成版本化数据集
       ↓
离线训练和独立验证
       ↓
新旧模型影子运行或A/B对比
       ↓
确认准确率和资源消耗后发布
```

每个AI事件需要记录：

- 视频源、设备和地区。
- 算法代码和模型版本。
- 事件时间和持续时间。
- 原始置信度。
- 当时使用的阈值、ROI、时段和规则。
- 截图和前后短视频。
- 人工确认、误报或漏报标签。
- 模型推理耗时和Worker信息。

数据集划分时应按摄像头、地点和时间隔离训练集与验证集，避免同一机位的相邻视频同时出现在训练和验证数据中，导致测试结果虚高。

模型升级后必须保留`model_version`，保证历史事件可以追溯到具体模型和配置。

## 21. 场景实施优先级建议

### 第一批：基础实时分析能力

- 禁区入侵。
- 越线检测。
- 人员逗留。
- 人数统计和聚集。
- 黑屏、冻结和遮挡。

这一批共用目标检测、跟踪、ROI、时段和事件合并能力，实施风险较低，并为所有后续算法建立实时数据通道。

### 第二批：明确目标检测

- 安全帽。
- 反光衣。
- 消防通道占用。
- 车辆违停。

这一批需要现场图片和困难负样本微调，但不需要复杂长时序模型。

### 第三批：消防和复杂动作

- 烟雾和火焰。
- 打架。
- 跌倒。
- 攀爬和危险作业动作。

这一批需要连续视频、较多现场标注、严格误报评估和更高GPU算力。

### 第四批：开放世界和大模型增强

- 任意异常发现。
- 开放词汇物体搜索。
- 大模型事件复核和自然语言描述。
- 历史视频语义检索。

这一批作为增强和辅助能力建设，不替代第一至第三批的专用模型、确定性规则与人工确认。
