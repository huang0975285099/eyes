# 复制为 .env 后填写。数据库由 docker-compose.yml 内的 MySQL 8.1.0 提供。
# 数据库：mysql:3306，账号 root，密码 all_seeing_eyes，库名 eyes。

# 必须与 app/config.json 的 apiKey 一致。
CLIENT_API_KEY=replace-me
STREAM_TOKEN_SECRET=replace-with-a-different-long-random-secret
PUBLIC_RTMP_HOST=112.18.238.6:21935

RECORDING_NODE_ID=recording-node-1
RECORDING_NODE_NAME=录像节点
RECORDING_SRS_HTTP_HOST=112.18.238.6:28080

# 留空表示抽帧只保存在本机，不推送到其他服务。
RECORDING_CENTER_URL=
RECORDING_CENTER_KEY=
